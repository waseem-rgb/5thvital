const HealthScreeningSection = () => {
  if (import.meta.env.DEV) {
    console.warn('[HealthScreeningSection] Deprecated: use PackagesSection instead.');
  }

  return null;
};

export default HealthScreeningSection;
